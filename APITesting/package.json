{
  "name": "apitesting",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "API smoke tests for preludeelectronics.com using Vitest and Allure",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "allure:generate": "node scripts/preserve-allure-history.js && allure generate allure-results --clean -o allure-report",
    "allure:open": "allure open allure-report",
    "allure:run": "npm run test && npm run allure:generate && npm run allure:open"
  },
  "keywords": [
    "api",
    "vitest",
    "allure",
    "prelude"
  ],
  "license": "ISC",
  "devDependencies": {
    "allure-commandline": "^2.38.1",
    "allure-vitest": "^3.7.2",
    "dotenv": "^17.4.2",
    "vitest": "^4.1.5"
  }
}
